import styled from "styled-components";
import {
  color,
  width,
  display,
  height,
  flex,
  alignItems,
  justifyContent,
  space,
  flexDirection,
  overflow,
  top,
  zIndex
} from "styled-system";

const TextWrapper = styled.div`
  ${color};
  ${width};
  ${display};
  ${height};
  ${flex};
  ${alignItems};
  ${justifyContent};
  ${space};
  ${flexDirection};
  ${overflow};
  ${top};
  ${zIndex};
`;

export default TextWrapper;
