import styled from "styled-components";
import {
  color,
  width,
  display,
  height,
  flex,
  alignItems,
  justifyContent,
  space
} from "styled-system";

const TextWrapper = styled.div`
  ${color};
  ${width};
  ${display};
  ${height};
  ${flex};
  ${alignItems};
  ${justifyContent};
  ${space};
`;

export default TextWrapper;
