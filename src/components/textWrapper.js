import styled from "styled-components";
import {
  color,
  width,
  display,
  height,
  flex,
  alignItems,
  justifyContent,
  space,
  flexDirection,
  flexWrap
} from "styled-system";

const TextWrapper = styled.div`
  ${color};
  ${width};
  ${display};
  ${height};
  ${flex};
  ${alignItems};
  ${justifyContent};
  ${space};
  ${flexDirection};
  ${flexWrap};
`;

export default TextWrapper;
